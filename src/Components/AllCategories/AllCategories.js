import { Typography } from '@mui/material';
import React from 'react';

const AllCategories = () => {
    return (
        <div>
            <Typography>
                All categories
            </Typography>
        </div>
    );
};

export default AllCategories;