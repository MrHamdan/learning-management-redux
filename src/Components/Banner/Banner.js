import { Container } from '@mui/material';
import React from 'react';

const Banner = () => {
    return (
        <div>
            <Container>

            </Container>
        </div>
    );
};

export default Banner;