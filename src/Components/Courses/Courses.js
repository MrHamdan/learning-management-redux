import { Container } from '@mui/material';
import React from 'react';

const Courses = () => {
    return (
        <div>
            <Container>
                
            </Container>
        </div>
    );
};

export default Courses;